import { BarcodeFindItem } from "./BarcodeFindItem";
import { BarcodeFindSession } from "./BarcodeFindSession";
export interface BarcodeFindListener {
    didStartSearch(): void;
    didPauseSearch(foundItems: BarcodeFindItem[]): void;
    didStopSearch(foundItems: BarcodeFindItem[]): void;
    didUpdateSession(session: BarcodeFindSession): void;
}
