import { CameraSettings, Brush } from "scandit-datacapture-frameworks-core";
import { BatterySavingMode } from "../barcode";
import { ScanIntention } from "scandit-datacapture-frameworks-core";
export interface BarcodeCaptureDefaults {
    BarcodeCaptureOverlay: {
        DefaultBrush: Brush;
    };
    BarcodeCaptureSettings: {
        codeDuplicateFilter: number;
        batterySaving: BatterySavingMode;
        scanIntention: ScanIntention;
    };
    RecommendedCameraSettings: CameraSettings;
}
export declare function getBarcodeCaptureDefaults(): BarcodeCaptureDefaults;
export declare function parseBarcodeCaptureDefaults(jsonDefaults: any): BarcodeCaptureDefaults;
