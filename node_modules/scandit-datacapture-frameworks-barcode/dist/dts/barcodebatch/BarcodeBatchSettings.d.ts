import { DefaultSerializeable } from "scandit-datacapture-frameworks-core";
import { SymbologySettings, Symbology } from "../symbology";
import { BarcodeBatchScenario } from "./BarcodeBatchScenario";
import { ArucoDictionary } from "../symbology";
export declare class BarcodeBatchSettings extends DefaultSerializeable {
    private scenario;
    private properties;
    private symbologies;
    private _arucoDictionary;
    get enabledSymbologies(): Symbology[];
    /**
    * @deprecated Setting a scenario is no longer recommended, use the BarcodeBatchSettings empty constructor instead.
    */
    static forScenario(scenario: BarcodeBatchScenario): BarcodeBatchSettings;
    private static get barcodeDefaults();
    constructor();
    settingsForSymbology(symbology: Symbology): SymbologySettings;
    setProperty(name: string, value: any): void;
    getProperty(name: string): any;
    enableSymbologies(symbologies: Symbology[]): void;
    enableSymbology(symbology: Symbology, enabled: boolean): void;
    setArucoDictionary(dictionary: ArucoDictionary): void;
}
