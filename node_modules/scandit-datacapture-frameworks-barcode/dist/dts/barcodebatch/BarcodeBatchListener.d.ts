import { FrameData } from "scandit-datacapture-frameworks-core";
import { BarcodeBatch } from "./BarcodeBatch";
import { BarcodeBatchSession } from "./BarcodeBatchSession";
export interface BarcodeBatchListener {
    didUpdateSession?(barcodeBatch: BarcodeBatch, session: BarcodeBatchSession, getFrameData: () => Promise<FrameData>): Promise<void>;
}
