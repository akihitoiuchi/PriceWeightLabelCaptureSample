import { DefaultSerializeable, DataCaptureMode, DataCaptureContext, CameraSettings, PrivateDataCaptureMode } from "scandit-datacapture-frameworks-core";
import { BarcodeBatchListener } from "./BarcodeBatchListener";
import { BarcodeBatchSettings } from "./BarcodeBatchSettings";
export declare class BarcodeBatch extends DefaultSerializeable implements DataCaptureMode {
    private type;
    private modeId;
    get isEnabled(): boolean;
    set isEnabled(isEnabled: boolean);
    get context(): DataCaptureContext | null;
    static get recommendedCameraSettings(): CameraSettings;
    private _isEnabled;
    private settings;
    private privateContext;
    private get _context();
    private set _context(value);
    private parentId;
    private listeners;
    private hasListeners;
    private listenerController;
    private isInListenerCallback;
    private static get barcodeBatchDefaults();
    static forContext(context: DataCaptureContext | null, settings: BarcodeBatchSettings): BarcodeBatch;
    private constructor();
    applySettings(settings: BarcodeBatchSettings): Promise<void>;
    addListener(listener: BarcodeBatchListener): void;
    removeListener(listener: BarcodeBatchListener): void;
    reset(): Promise<void>;
}
export interface PrivateBarcodeBatch extends PrivateDataCaptureMode {
    _context: DataCaptureContext | null;
    listeners: BarcodeBatchListener[];
    isInListenerCallback: boolean;
    parentId: number | null;
}
