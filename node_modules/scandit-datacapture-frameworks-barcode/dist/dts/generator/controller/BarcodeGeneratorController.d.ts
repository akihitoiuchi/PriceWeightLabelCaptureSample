import { NativeCallResult } from "scandit-datacapture-frameworks-core";
import { BarcodeGenerator } from "../BarcodeGenerator";
export interface BarcodeGeneratorProxy {
    create(barcodeGeneratorJson: string): Promise<void>;
    dispose(generatorId: string): Promise<void>;
    generateFromBase64EncodedData(generatorId: string, data: string, imageWidth: number): Promise<NativeCallResult>;
    generate(generatorId: string, text: string, imageWidth: number): Promise<NativeCallResult>;
}
export declare class BarcodeGeneratorController {
    private generator;
    private get _proxy();
    static forBarcodeGenerator(generator: BarcodeGenerator): BarcodeGeneratorController;
    initialize(): Promise<void>;
    create(): Promise<void>;
    generateFromBase64EncodedData(data: string, imageWidth: number): Promise<string>;
    generate(text: string, imageWidth: number): Promise<string>;
    dispose(): Promise<void>;
}
