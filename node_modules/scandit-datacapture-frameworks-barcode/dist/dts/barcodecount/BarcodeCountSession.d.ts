import { DefaultSerializeable } from "scandit-datacapture-frameworks-core";
import { Barcode, BarcodeJSON } from "../barcode/Barcode";
import { BarcodeSpatialGrid } from "../barcode";
import { BarcodeCountSessionEventPayload } from "./controller/BarcodeCountViewEventHandlers";
export declare class BarcodeCountSession extends DefaultSerializeable {
    private _recognizedBarcodes;
    private _additionalBarcodes;
    private _frameSequenceID;
    private sessionController;
    private frameId;
    private static fromJSON;
    private constructor();
    get recognizedBarcodes(): Barcode[];
    get additionalBarcodes(): Barcode[];
    get frameSequenceID(): number;
    reset(): Promise<void>;
    getSpatialMap(): Promise<BarcodeSpatialGrid | null>;
    getSpatialMapWithHints(expectedNumberOfRows: number, expectedNumberOfColumns: number): Promise<BarcodeSpatialGrid | null>;
}
export interface BarcodeCountSessionJSON {
    recognizedBarcodes: BarcodeJSON[];
    additionalBarcodes: Barcode[];
    frameSequenceId: number;
}
export interface PrivateBarcodeCountSession {
    fromJSON(json: BarcodeCountSessionEventPayload): BarcodeCountSession;
    frameId: string;
}
