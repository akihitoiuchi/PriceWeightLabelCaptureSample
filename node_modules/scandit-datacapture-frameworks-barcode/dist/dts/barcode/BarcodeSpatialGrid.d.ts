import { BarcodeJSON } from './Barcode';
import { Barcode } from './Barcode';
import { DefaultSerializeable } from 'scandit-datacapture-frameworks-core';
export interface BarcodeSpatialGridElementJSON {
    mainBarcode: BarcodeJSON;
    subBarcode: BarcodeJSON | null;
}
export declare class BarcodeSpatialGrid extends DefaultSerializeable {
    private _rows;
    private _columns;
    private _grid;
    private static fromJSON;
    get rows(): number;
    get columns(): number;
    barcodeAt(row: number, column: number): Barcode | null;
    row(index: number): Barcode[];
    column(index: number): Barcode[];
}
export interface BarcodeSpatialGridJSON {
    rows: number;
    columns: number;
    grid: BarcodeSpatialGridElementJSON[][];
}
export interface PrivateBarcodeSpatialGrid {
    fromJSON(json: BarcodeSpatialGridJSON): BarcodeSpatialGrid;
}
