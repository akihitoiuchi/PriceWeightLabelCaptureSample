import { DefaultSerializeable } from "scandit-datacapture-frameworks-core";
import { BarcodePickState } from "../BarcodePickState";
export declare class IconForStateObject extends DefaultSerializeable {
    private readonly _barcodePickState;
    private readonly _icon;
    constructor(barcodePickState: BarcodePickState, icon: string);
    get barcodePickState(): BarcodePickState;
    get icon(): string;
}
