import { BarcodePick } from "./BarcodePick";
import { BarcodePickSession } from "./BarcodePickSession";
export interface BarcodePickListener {
    didUpdateSession(barcodePick: BarcodePick, session: BarcodePickSession): void;
}
