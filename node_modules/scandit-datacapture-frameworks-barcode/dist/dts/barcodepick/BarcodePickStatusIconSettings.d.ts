import { DefaultSerializeable } from "scandit-datacapture-frameworks-core";
export declare class BarcodePickStatusIconSettings extends DefaultSerializeable {
    private _ratioToHighlightSize;
    get ratioToHighlightSize(): number;
    set ratioToHighlightSize(value: number);
    private _minSize;
    get minSize(): number;
    set minSize(value: number);
    private _maxSize;
    get maxSize(): number;
    set maxSize(value: number);
    private static get barcodePickDefaults();
    private static fromJSON;
}
export interface PrivateBarcodePickStatusIconSettings {
    fromJSON(json: any): BarcodePickStatusIconSettings;
}
