import { Barcode } from "../barcode/Barcode";
import { Brush, Observable, ScanditIcon } from "scandit-datacapture-frameworks-core";
import { BarcodeArHighlight } from "./BarcodeArHighlight";
export declare class BarcodeArRectangleHighlight extends Observable implements BarcodeArHighlight {
    private static get barcodeArDefaults();
    private _barcode;
    private _type;
    private _brush;
    private _icon;
    constructor(barcode: Barcode);
    get barcode(): Barcode;
    get brush(): Brush;
    set brush(brush: Brush);
    get icon(): ScanditIcon | null;
    set icon(icon: ScanditIcon | null);
}
