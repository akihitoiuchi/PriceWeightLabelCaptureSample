import { DefaultSerializeable, Feedback } from "scandit-datacapture-frameworks-core";
import { BarcodeArViewController } from "./controller/BarcodeArViewController";
export declare class BarcodeArFeedback extends DefaultSerializeable {
    static get defaultFeedback(): BarcodeArFeedback;
    private controller;
    private static get barcodeArDefaults();
    private static fromJSON;
    private _scanned;
    private _tapped;
    get scanned(): Feedback;
    set scanned(scanned: Feedback);
    get tapped(): Feedback;
    set tapped(tapped: Feedback);
    private updateFeedback;
    constructor();
}
export interface BarcodeArFeedbackJSON {
    scanned: Feedback;
    tapped: Feedback;
}
export interface PrivateBarcodeArFeedback {
    controller: BarcodeArViewController | null;
}
