import { Color, FontFamily, Observable, ScanditIcon } from "scandit-datacapture-frameworks-core";
export declare class BarcodeArPopoverAnnotationButton extends Observable {
    private static get barcodeArDefaults();
    private _textColor;
    private _textSize;
    private _fontFamily;
    private _icon;
    private _text;
    constructor(icon: ScanditIcon, text: string);
    get textColor(): Color;
    set textColor(value: Color);
    get textSize(): number;
    set textSize(value: number);
    get fontFamily(): FontFamily;
    set fontFamily(value: FontFamily);
    get icon(): ScanditIcon;
    get text(): string;
}
