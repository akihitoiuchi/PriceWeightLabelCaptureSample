import { Brush, Observable } from "scandit-datacapture-frameworks-core";
import { ScanditIcon } from "scandit-datacapture-frameworks-core";
import { Barcode } from "../barcode";
import { BarcodeArCircleHighlightPreset } from "./BarcodeArCircleHighlightPreset";
import { BarcodeArHighlight } from "./BarcodeArHighlight";
export declare class BarcodeArCircleHighlight extends Observable implements BarcodeArHighlight {
    private static get barcodeArDefaults();
    private _type;
    private _barcode;
    private _brush;
    private _icon;
    private _preset;
    private _size;
    constructor(barcode: Barcode, preset: BarcodeArCircleHighlightPreset);
    get barcode(): Barcode;
    get brush(): Brush;
    set brush(brush: Brush);
    get icon(): ScanditIcon | null;
    set icon(value: ScanditIcon | null);
    get size(): number;
    set size(value: number);
}
