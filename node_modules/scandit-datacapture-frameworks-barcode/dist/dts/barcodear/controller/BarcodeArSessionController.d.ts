import { BaseNewController } from "scandit-datacapture-frameworks-core";
export interface BarcodeArSessionProxy {
    $resetBarcodeArSession({ viewId }: {
        viewId: number;
    }): Promise<void>;
}
export declare class BarcodeArSessionController extends BaseNewController<BarcodeArSessionProxy> {
    private viewId;
    constructor(viewId: number);
    $resetBarcodeArSession(): Promise<void>;
}
