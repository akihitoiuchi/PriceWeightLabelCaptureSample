import { Color, Observable, ScanditIcon } from 'scandit-datacapture-frameworks-core';
import { BarcodeArAnnotation } from './BarcodeArAnnotation';
import { Barcode } from '../barcode';
import { BarcodeArAnnotationTrigger } from './BarcodeArAnnotationTrigger';
export declare class BarcodeArStatusIconAnnotation extends Observable implements BarcodeArAnnotation {
    private static get barcodeArDefaults();
    private _type;
    private _barcode;
    private _hasTip;
    private _icon;
    private _text;
    private _textColor;
    private _backgroundColor;
    private _annotationTrigger;
    constructor(barcode: Barcode);
    get barcode(): Barcode;
    get hasTip(): boolean;
    set hasTip(value: boolean);
    get icon(): ScanditIcon;
    set icon(value: ScanditIcon);
    get text(): string | null;
    set text(value: string | null);
    get textColor(): Color;
    set textColor(value: Color);
    get backgroundColor(): Color;
    set backgroundColor(value: Color);
    get annotationTrigger(): BarcodeArAnnotationTrigger;
    set annotationTrigger(value: BarcodeArAnnotationTrigger);
}
