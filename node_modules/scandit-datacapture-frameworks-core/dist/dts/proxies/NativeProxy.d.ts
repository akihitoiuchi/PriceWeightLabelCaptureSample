import { BaseInstanceAwareNativeProxy } from "./BaseInstanceAwareNativeProxy";
import { NativeCaller } from "./NativeCaller";
export declare class NativeProxy extends BaseInstanceAwareNativeProxy {
    protected nativeCaller: NativeCaller;
    protected eventSubscriptions: Map<string, any>;
    private cachedEventHandler;
    [k: string]: any;
    constructor(nativeCaller: NativeCaller);
    get framework(): string;
    get frameworkVersion(): string;
    subscribeForEvents(events: string[]): Promise<void>;
    unsubscribeFromEvents(events: string[]): Promise<void>;
    dispose(): Promise<void>;
    _call(fnName: string, args: object | undefined | null): Promise<any>;
    private _registerEvent;
    private _unregisterEvent;
}
export declare function createNativeProxy<PROXY>(nativeCaller: NativeCaller): PROXY;
